name: CI-CD Simples

on:
  push:
    branches: ["main"]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checar código
        uses: actions/checkout@v3

      - name: Build backend
        run: docker build -t backend -f Dockerfile.back .

      - name: Build frontend
        run: docker build -t frontend -f Dockerfile.front .

      - name: Finalizado
        run: echo "Pipeline executada com sucesso!"
